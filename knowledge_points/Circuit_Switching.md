# 知识点复习：Circuit Switching (电路交换)

## 📌 考频分析 (Testing Frequency)
- **考频评级**：**低频考点 (Low Frequency)**
- **复习建议**：**了解定义及对比**
- **试卷覆盖情况**：
  - 我已详细查阅了系统内提供的所有试卷（包含 `old_exams` 文件夹以及 `old_exams-grnvs` 文件夹中的德英双语及中文解析等）。
  - 该知识点仅出现在两份历年试卷中（`retake_2012` 和 `retake_2022`）。
  - 它不是计算题的重点，考法极为单一和固定。只需背诵特定的对比答案即可拿满分，不需要在此花费过多时间。

---

## 📚 考点总结 (Topic Overview)
在 `slides_chap3.pdf` 中，**Circuit switching (电路交换)** 被定义为在发送方和接收方之间保留/预留一条专用的物理或逻辑线路 (Reserve a dedicated line between transmitter and receiver)。

它的整个通信生命周期严格包含**三个阶段** (Phases)：
1. **Connection setup (连接建立)**：通过交换信令消息 (signalling messages) 来建立一条专用连接。
2. **Data transfer (数据传输)**：连接建立后开始传输数据。
3. **Connection teardown (连接拆除)**。

**它的核心优势 (Advantages)**：
- **质量稳定**：建立连接后，专用连接的质量是一致且有保障的 (Consistent quality)。
- **传输速度快**：在传输数据时，中间节点不需要做任何路由/交换决策 (fast data transfer without the need to make further switching decisions)。

---

## ⏱️ 核心公式深挖：Transmission Delay (传输延迟计算)
这是我之前梳理遗漏的极为核心的考点（位于 `slides_chap3.pdf` 第 8 页及 19 页）！
所有 Switching Modes 的灵魂都在于**延迟时间的计算**。虽然真题更偏爱考 Packet vs Message Switching 的时间计算，但 Circuit Switching 的延迟公式是所有对比的基础。

**公式：**
$$ T_{CS} = \frac{L}{r} + \frac{4d}{v_{C0}} $$
*(假设 `n=2` 个中间节点，忽略处理时间和信令消息的序列化时间)*

**公式参数拆解：**
- **$L/r$**: 数据的发送时间 (Data Transmission/Serialization Time)。其中 $L$ 为数据总长度，$r$ 为数据率 (Data rate)。
- **$4d/v_{C0}$**: 传播延迟 (Propagation Delay)。其中 $d$ 为距离，$v_{C0}$ 为光速。为什么是 4 倍（假设源节点到目的节点是 $d$）？因为它包含了**连接建立 (Connection Setup)** 的往返时间以及**真实数据**的传输和拆除阶段的交互信号传播。

**对比其他模式 (Slide 19)：**
- **Message switching**: $T_{MS} = (n+1)\frac{L_h + L}{r} + \frac{d}{v_{c0}}$ （有多个跳步的存储转发延迟）
- **Packet switching**: $T_{PS} = \frac{1}{r} (\lceil \frac{L}{p_{max}} \rceil \cdot L_h + L) + \frac{d}{v_{c0}} + n \cdot L_h + \frac{p_{max}}{r}$

---

## 🎯 考法分析与预测 (How it is tested & Potential variations)
纵观历年试卷，对于 Circuit Switching 的考查**非常专一**，几乎全部集中在它与 **Packet switching (分组交换)** 的**对比**上。

考试时经常会出以下两种简答题，并且**可能出现的拓展考法**包括：
1. **概念区分题 (经典必考)**：要求你一句话直接说明 Circuit switching 和 Packet switching 的根本区别。
   - *拓展考法*：除了直接问区别，可能会给出一个具体场景（如传输一个大文件 vs 偶尔发送的传感器数据），问你哪种交换模式更合适并解释原因。
2. **优缺点对比题 (经典必考)**：要求你从**链路利用率 (utilization)** 或**容错性 (fault tolerance)** 的角度，阐述 Packet switching 相比于 Circuit switching 的优势。
   - *拓展考法*：反过来问你 Circuit switching 的优势（答案是：稳定的传输质量 / 节点不需要作路由决策），务必也要记牢这一点！

---

## 📝 历年原题与答案解析 (Past Questions & Answers)

### 题目 1：概念区别 (The Difference)
**Question:** What is the difference between circuit switching and packet switching?
**Answer:** 
- **1 Circuit switching** = reservation of a dedicated line (physically or logically) between sender and receiver.
- **2 Packet switching** = sending independent packets with their own addressing information.

📍 **出处 (Source):** *[retake_2012_en.md，Task 2e]*

💡 **中文解析与给分点:** 
这是一道非常经典的直接定义题。答题的**必踩得分点**在于：
- 对于 **Circuit switching**，必须写出核心词 **"dedicated line" (专用线路)** 并且提到了建立的过程如 **"reservation" (预留)**。
- 对于 **Packet switching**，必须写出核心词 **"independent packets" (独立的分组/包)** 以及它们不依赖预留路径，而是自带 **"addressing information" (地址信息)**。

---

### 题目 2：对比优势 (The Advantage)
**Question:** Explain the advantage of packet switching over circuit switching.
**Answer:** Packet switching achieves a better utilization of the link. It does not block the link while a connection is established but no data is sent. 
**OR** It is more fault-tolerant, as packets can be routed around defective links.

📍 **出处 (Source):** *[retake_2022_en.md，Task 2j / retake_2022-solution_en.md，Task 2k]*

💡 **中文解析与给分点:** 
这是最常考的深入对比题。官方答案给出了两条得分路径，你考试时**任选其一（或全写保障）**即可：
1. **得分路径 A：从资源利用率角度 (Utilization)**。必须提到分组交换拥有更高的链路利用率（better utilization）。因为电路交换在建立连接后，即使用户不发数据，这条线路也是被“锁定(block)”的；而分组交换是按需发送的，资源不闲置。
2. **得分路径 B：从容错性角度 (Fault-tolerant)**。提到分组交换更具容错性。如果网络中某条链路断了 (defective links)，独立的数据包可以临时改变路由绕开故障区；但在电路交换中，中间链路一断，整个连接彻底瘫痪。
