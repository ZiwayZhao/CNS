# 知识点精讲：物理层 L1 的采样 (Sampling) 与量化 (Quantization) 🎛️

在物理层 (Layer 1) 的考察体系中，模拟信号 (Analog) 转换为数字信号 (Digital) 的过程是历年必考的概念题和小型计算题。针对你在系统复习中提出的疑惑，我已经从海量历年考试中提取了这两者的**定义辨析题**和**量化参数计算题**。

---

## 考点一：概念辨析的“黄金一句话” (必考填空/简答)

这是整个物理层最爱考的一字不差的简答题。
- **真题重现**: *Explain the difference between sampling and quantization.* (请解释采样与量化的区别。)
- **满分标准答案**:
  - **Sampling (采样)**: Discretization in the **time domain** (时间域上的离散化).
  - **Quantization (量化)**: Discretization in the **value/amplitude domain** with rounding (数值/幅度域上的离散化并伴随四舍五入).

**教授的干扰项防坑点**：
- 如果题目问：“对连续信号 $s(t)$ 进行**采样**会得到什么？” 
  - 答：Time-discrete and value-continuous signal (时间离散、值仍然连续的信号).
- 如果题目问：“对其再进行**量化**得到什么？”
  - 答：Time-discrete and value-discrete signal (时间离散、值也离散的彻底的数字信号).

---

## 考点二：最核心的“量化计算题”三步法 (压轴必会)

量化的计算题无非就是给你“区间(Interval)”、“误差(Error)”或“比特数(Bits)”，让你倒推其中的某一项。所有题目的底层逻辑都遵循下面这个黄金倒三角公式。

### 【神仙三角公式】
- **量化范围 (Range)**: $R = \text{Upper Bound} - \text{Lower Bound}$ 
- **量化阶数/层级数量 (Levels / $M$)**: $M = 2^{\text{Bits}}$
- **步长 (Step Size / $\Delta$)**: $\Delta = \frac{R}{M}$
- **最大量化误差 (Max Quantization Error / $e_{\text{max}}$)**: $e_{\text{max}} = \frac{\Delta}{2} = \frac{R}{2 \times M}$

---

### 真题套路 1："求最少量化层级数" (The Minimum Levels Calculation)
- **真题重现 (Midterm 2022)**: *A value-continuous signal is to be quantized in the interval $I = [-3, 3]$ so that the quantization error within $I$ is at most $0.4$. How many quantization levels are required at a minimum?*
- **破解步骤**:
  1. 算出目标范围: $R = 3 - (-3) = 6$。
  2. 根据公式 $\Delta = 2 \times e_{\text{max}}$，我们被允许的最大步长为 $2 \times 0.4 = 0.8$。
  3. 用范围除以步长要求：$M = \frac{R}{\Delta} = \frac{6}{0.8} = 7.5$。
  4. **硬核规矩**：层数必须是整数，且不能比要求少（否则误差包不住），所以必须做**向上取整 (Ceiling)**。
  5. 结果: $\lceil 7.5 \rceil = \mathbf{8 \text{ levels}}$。

- **举一反三 (Quiz 1 2017)**: *Interval $I = [-2, 2]$, Max Error $\le 0.5$.*
  - $R = 4$, $\Delta \le 1.0$.
  - $M = \lceil \frac{4}{1.0} \rceil = \mathbf{4 \text{ levels}}$。

---

### 真题套路 2："求量化误差" (The Max Error Calculation)
- **真题重现 (Retake 2020)**: *Given is a continuous signal that is to be quantized linearly with 3 bits in the interval $I = [7, 16]$. Determine the maximum quantization error within $I$.*
- **破解步骤**:
  1. 根据 bit 数算出阶数：$3 \text{ bits} \implies M = 2^3 = 8 \text{ levels}$。
  2. 算出范围: $R = 16 - 7 = 9$。
  3. 算出步长: $\Delta = \frac{9}{8} = 1.125$。
  4. 误差就是步长的一半: $e_{\text{max}} = \frac{1.125}{2} = \mathbf{0.5625}$。

只要你能在考场上死记硬背这个映射关系：**误差是步长的一半 ($e_{\text{max}} = \Delta / 2$)**，结合**向上取整保证精度**的绝对逻辑，这类量化计算小题绝对是手到擒来，秒杀得分点！
